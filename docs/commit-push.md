# 커밋 푸시 규칙

커밋 푸시 요청을 받으면 현재 작업 디렉토리의 모든 변경 파일을 커밋에 포함한다.

- 수정 파일, 삭제 파일, 새 파일을 모두 포함
- 특정 파일을 제외해야 하면 사용자가 명시한 경우에만 제외
- 커밋 전 `git status`로 포함 대상 확인
- 커밋 메시지는 한글로 작성
- 커밋 후 `git push` 실행
- 푸시 후 남은 변경이 있는지 `git status`로 확인
